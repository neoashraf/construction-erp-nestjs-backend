name: Deploy to Hostinger (self-hosted)

on:
  push:
    branches: [dev]
  workflow_dispatch:

concurrency:
  group: ze-erp-backend-deploy
  cancel-in-progress: true

jobs:
  test:
    name: Typecheck, lint & test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test

  deploy:
    name: Build and restart ze-erp-backend
    needs: test
    runs-on: [self-hosted, linux, ze-erp-vps]

    steps:
      - name: Debug runner
        shell: bash
        run: |
          echo "================================="
          echo "RUNNER_NAME=$RUNNER_NAME"
          echo "HOSTNAME=$(hostname)"
          echo "USER=$(whoami)"
          echo "HOME=$HOME"
          echo "PWD=$(pwd)"
          echo "================================="

      - name: Checkout latest code
        uses: actions/checkout@v4

      - name: Verify env file
        shell: bash
        run: |
          echo "===== ENV FILE CHECK ====="
          ls -ld /opt/ze-erp || true
          ls -lah /opt/ze-erp/.env || true

          if [ ! -f /opt/ze-erp/.env ]; then
            echo "Server env file not found at /opt/ze-erp/.env"
            exit 1
          fi

          echo "Env file present"

      - name: Build and restart containers
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          docker compose --env-file /opt/ze-erp/.env up -d --build --remove-orphans

      - name: Wait for health check
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          for attempt in $(seq 1 20); do
            if curl -sf http://localhost:7001/health >/dev/null; then
              echo "Application is healthy"
              exit 0
            fi

            echo "Waiting for app... attempt ${attempt}/20"
            sleep 3
          done

          echo "Health check failed"

          docker compose --env-file /opt/ze-erp/.env ps || true
          docker compose --env-file /opt/ze-erp/.env logs --tail=100 ze-erp-backend || true

          exit 1

      - name: Show running containers
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          docker compose --env-file /opt/ze-erp/.env ps

      - name: Prune old images
        shell: bash
        run: |
          docker image prune -f
